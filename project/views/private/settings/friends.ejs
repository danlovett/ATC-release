<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Friends</title>
</head>
<body class="bg-blue">
    <%- include('../navbar.ejs') -%>
    <div class="sections">
        <div>
            <h1 class="page-title">Friends</h1>
            <p style="padding: 10px; text-align: center;">See all friend details here</p>
        </div>
        <section class="profile settings main-section">
            <%- include('sidebar.ejs') -%>
            <%_ if(JSON.stringify(followers) == '[]' && JSON.stringify(following) == '[]' && JSON.stringify(follower_requests) == '[]' && JSON.stringify(requested_followers) == '[]') { %>
                <p style="text-align: center; margin-top: auto;">Nothing here. Either wait for requests or <a href="/search">search</a> for people.</p>
            <%_ } else { %>
                <div class="content">
                    <%_ if(JSON.stringify(follower_requests) != '[]') { %>
                        <section>
                            <h1 class="title">Incoming requests</h1>
                            <table>
                                <thead>
                                    <tr>
                                        <th>Person</th>
                                        <th>Accept</th>                                    
                                    </tr>
                                </thead>
                                <tbody>
                                    <%_ for(i in follower_requests) { %>
                                        <tr>
                                            <td id="person">
                                                <img src="<%= follower_requests[i].pfp %>" alt="">
                                                <a class="sidebar" href="/profile/<%= follower_requests[i].id %>">
                                                    <p><%= follower_requests[i].name %></p>
                                                    <p><%= follower_requests[i].username %></p>
                                                </a>
                                            </td>
                                            <td>
                                                <a href="/accept_request/<%= follower_requests[i].id %>" class="transform-120">Accept</a>
                                                <a href="/reject_friend/<%= follower_requests[i].id %>" class="transform-120">Reject</a>
                                            </td>
                                        </tr>
                                    <%_ } %>
                                </tbody>
                            </table>
                        </section>
                    <%_ } %>
                    <%_ if(JSON.stringify(following) != '[]') { %>
                        <section class="followers">
                            <h1 class="title">Following</h1>
                            <table>
                                <thead>
                                    <tr>
                                        <th>Person</th>
                                        <th>Since</th>                                    
                                    </tr>
                                </thead>
                                <tbody>
                                    <%_ for(i in following) { %>
                                        <tr>
                                            <td id="person">
                                                <img src="<%= following[i].pfp %>" alt="">
                                                <a class="sidebar" href="/profile/<%= following[i].id %>">
                                                    <p><%= following[i].name %></p>
                                                    <p><%= following[i].username %></p>
                                                </a>
                                            </td>
                                            <td><%= following[i].creation_date.substr(0, 10) %></td>
                                            <td><a href="/remove_friend/<%= following[i].id %>" class="transform-120">X</a></td>
                                        </tr>
                                    <%_ } %>
                                </tbody>
                            </table>
                        </section>
                    <%_ } %>
                    <%_ if(JSON.stringify(followers) != '[]') { %>
                        <section class="followers">
                            <h1 class="title">Followers</h1>
                            <table>
                                <thead>
                                    <tr>
                                        <th>Person</th>
                                        <th>Since</th>                                    
                                    </tr>
                                </thead>
                                <tbody>
                                    <%_ for(i in followers) { %>
                                        <tr>
                                            <td id="person">
                                                <img src="<%= followers[i].pfp %>" alt="">
                                                <a class="sidebar" href="/profile/<%= followers[i].id %>">
                                                    <p><%= followers[i].name %></p>
                                                    <p><%= followers[i].username %></p>
                                                </a>
                                            </td>
                                            <td><%= followers[i].creation_date.substr(0, 10) %></td>
                                            <td><a href="/remove_friend/<%= followers[i].id %>" class="transform-120">X</a></td>
                                        </tr>
                                    <%_ } %>
                                </tbody>
                            </table>
                        </section>
                    <%_ } %>
                    <%_ if(JSON.stringify(requested_followers) != '[]') { %>
                        <section class="followers">
                            <h1 class="title">Sent Requests</h1>
                            <table>
                                <thead>
                                    <tr>
                                        <th>Person</th>
                                        <th>Remove?</th>                                    
                                    </tr>
                                </thead>
                                <tbody>
                                    <%_ for(i in requested_followers) { %>
                                        <tr>
                                            <td id="person">
                                                <img src="<%= requested_followers[i].pfp %>" alt="">
                                                <a class="sidebar" href="/profile/<%= requested_followers[i].id %>">
                                                    <p><%= requested_followers[i].name %></p>
                                                    <p><%= requested_followers[i].username %></p>
                                                </a>
                                            </td>
                                            <td><a href="/remove_request/<%= requested_followers[i].id %>" id="remove-request" class="transform-120">X</a></td>
                                        </tr>
                                    <%_ } %>
                                </tbody>
                            </table>
                        </section>
                    <%_ } %>
                    </div>
            <%_ } %>
        </section>
    </div>
</body>
</html>